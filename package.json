{
  "name": "justinelonglat-lane-blogs",
  "private": true,
  "version": "1.0.0",
  "type": "module",
"scripts": {
  "prebuild": "node scripts/generate-indexes.mjs",
  "build": "node scripts/generate-indexes.mjs && node scripts/vercel-dist.mjs",
  "validate:links": "node scripts/validate-internal-links.mjs",
  "serve": "npx serve dist -l 3000"
  },
  "devDependencies": {
    "cheerio": "^1.1.2",
    "fast-glob": "^3.3.3",
    "globby": "^13.2.2",
    "gray-matter": "^4.0.3",
    "htmlparser2": "^10.0.0",
    "serve": "^14.2.5",
    "slugify": "^1.6.6",
    "vite": "^7.2.7"
  }
}
